# 运行时设置
双击页面上方标题栏，即可打开设置菜单。

## 视频设置
点击 `设置` - `视频`。

### 画面品质
Unity 有六个画质级别(`QualityLevel`)，分别是 `Very Low` `Low` `Medium` `High` `Very High` `Ultra`。Asteroid 默认为 `High` 级别画质。

您可以通过滑动滑块条来调整画质。

### 摄像机旋转速度
您可以通过滑动滑块来调整场景中摄像机的旋转速度。

## 音频设置
您可以通过滑动滑块来调整主音量。